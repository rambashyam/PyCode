def calculatePy(a:List[List[int]):
	
	for i in range(len(a)):
		for j in range(len(a[0]):
			print(a[i][j])
			print(a[j[i])
	print("Exiting the for loop")

# this is some random pycode I wrote for checking out GH
	
